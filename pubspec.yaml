
name: zipride_rider_app
description: A bike taxi rider app like Rapido, but with Zipride features.
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.0.0
  firebase_auth: ^4.0.0
  cloud_firestore: ^4.0.0
  provider: ^6.0.0
  geolocator: ^9.0.0
  google_maps_flutter: ^2.0.0
  url_launcher: ^6.0.0
  flutter_sms: ^2.3.1
  flutter_chat_ui: ^1.5.6
  flutter_rating_bar: ^4.0.0
  shared_preferences: ^2.0.15
  intl: ^0.18.0
  http: ^0.14.0
flutter:
  uses-material-design: true
